# Env principal "sam3" (pour run_segmentation.py + benchmark des méthodes sam3_*).
# Installer d'abord PyTorch adapté à votre CUDA : https://pytorch.org
transformers==5.15.0
huggingface_hub
accelerate
numpy>=2.0             # requis pour la lecture rapide via tifffile (reshape(copy=) ;
                       # sinon le code retombe sur PIL, ~5x plus lent)
opencv-python
tifffile
scikit-image
scipy
pillow
tqdm
matplotlib            # figures du benchmark
